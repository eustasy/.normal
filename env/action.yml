name: Normal Env
description: Lint dotenv files with dotenv-linter.

runs:
  using: composite
  steps:
    - uses: qltysh/qlty-action/install@08a0a862c159eae9b9003081da6663d96efef637 # v2.3.0

    - name: Lint (dotenv-linter)
      shell: bash
      run: qlty check --all --filter=dotenv-linter
